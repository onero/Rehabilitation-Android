<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!--Daily Exercise Notification Switch-->
    <SwitchPreference
    android:defaultValue="true"
    android:id="@+id/dailyExerciseSwitch"
    android:key="notifications_daily_exercises"
    android:title="@string/pref_title_daily_exercises"/>

    <!--Daily Exercise Time Chooser-->
    <Preference
        android:title="Set your preferred notification time"/>
    <Preference
        android:title="12:00"
        android:defaultValue="12:00"
        android:icon="@drawable/time"
        android:key="@string/pref_key_set_notification_time"/>

    <!-- A 'parent' preference, which enables/disables child preferences (below)
         when checked/unchecked. -->

    <!--Entire section left for inspiration with new exercise notifications!-->
    <!--<SwitchPreference-->
        <!--android:defaultValue="true"-->
        <!--android:key="notifications_new_message"-->
        <!--android:title="@string/pref_title_new_message_notifications"/>-->

    <!-- Allows the user to choose a ringtone in the 'notification' category. -->
    <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
    <!-- NOTE: RingtonePreference's summary should be set to its value by the activity code. -->
    <!--<RingtonePreference-->
        <!--android:defaultValue="content://settings/system/notification_sound"-->
        <!--android:dependency="notifications_new_message"-->
        <!--android:key="notifications_new_message_ringtone"-->
        <!--android:ringtoneType="notification"-->
        <!--android:title="@string/pref_title_ringtone"/>-->

    <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
    <!--<SwitchPreference-->
        <!--android:defaultValue="true"-->
        <!--android:dependency="notifications_new_message"-->
        <!--android:key="notifications_new_message_vibrate"-->
        <!--android:title="@string/pref_title_vibrate"/>-->

</PreferenceScreen>
